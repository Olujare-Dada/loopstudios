{
  "buildCommand": "cd loopstudios-project && npm install && npm run build",
  "outputDirectory": "loopstudios-project",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
