{
  "buildCommand": "cd loopstudios-projects && npm install && npm run build",
  "outputDirectory": "loopstudios-projects",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
